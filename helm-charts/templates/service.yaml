apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-sec-board.fullname" . }}
  labels:
    {{- include "kube-sec-board.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-sec-board.selectorLabels" . | nindent 4 }}
